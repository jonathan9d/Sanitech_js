# Dépendances
node_modules/
npm-debug.log*

# Sorties de build (générées depuis les sources à la racine)
www/
dist/
dist-electron/

# Binaires
*.apk
*.exe
*.msi

# Android (signature & config locale)
android/keystore/
android/keystore.properties

# Données locales / runtime
server-data/
*.log
.DS_Store
Thumbs.db

# Outil de conversion de polices (local)
.venv-font/

# Compilation ESP32 : bibliothèque esp32-camera téléchargée en local pour arduino-cli
.arduino-tmp/
